import React from 'react';
import { UIView } from '../UI/UI';

export const TemplatesView: React.FC = () => {
    return (
        <UIView title="Note Templates 📄">
            <p>This is the updated templates view.</p>
        </UIView>
    );
};
