import React from 'react';
import {UIView} from '../UI/UI';

export const TemplatesView: React.FC = () => {
    return (
        <UIView title="Note Templates (Stubbed) 📄">
            <p>Note templates management and creation will be implemented here.</p>
        </UIView>
    );
};