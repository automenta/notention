import React from 'react';
import {UIView} from '../UI/UI';

export const SettingsView: React.FC = () => {
    return (
        <UIView title="System Settings (Stubbed) ⚙️">
            <p>System settings and configurations will be implemented here.</p>
        </UIView>
    );
};